# USER.md

改用户能看见的文案、入口或流程时读本文件：使用者是谁、他们要办成什么、界面用哪些词。安装步骤在 `README.md`；模块划分、消息和红线的代码落点在 `AGENTS.md`。

## 使用者

在 Chrome、Edge 或夸克里找工作的人。岗位页是他们自己打开的，安全验证、打招呼和提交也由他们亲手完成。

他们常同时比较很多岗位，在 BOSS 直聘的岗位详情、推荐/搜索列表，以及其他招聘网站之间切换。简历可以有多份，其中一份标为默认，用来填当前页。模型的接口地址、名称和密钥由他们自己填写，只留在这个浏览器里。

## 要办成的事

1. **收下当前已经打开的岗位。** 详情页提取这一条。推荐/搜索列表页列出本页卡片，可逐条收藏或全部收藏；插件不翻页，滚出新卡片后点「重新扫描」。列表卡片只有职位名称、薪资、工作地点、经验、学历、标签、公司名和链接，职位描述等到打开原岗位再补。页面停在安全验证时，先在网页里手动完成，再提取。
2. **在本机管理收藏。** 搜索职位、公司或链接，按投递状态筛选，打开原岗位，导出 Markdown 或 JSON，或清空。其他网站的岗位用「导入岗位」贴进来，和 BOSS 收藏放在一起。同一岗位再收藏会更新内容，并保留第一次收藏时间。
3. **用默认简历填写当前招聘页。** 只写入这一页的空表单，并把简历文件放进上传框。填完后由他们自己检查、再决定是否提交。
4. **按目标和收藏生成建议。** 先写下目标职位和必须条件。不勾选岗位时，取和目标更接近的最多 6 条；勾选后只分析勾选的，仍最多 6 条。四类结果：岗位推荐、简历修改、投递建议、面试准备。结果留在本机，下次可直接打开。
5. **记下投递和面试。** 投递状态为：已投、约面、终面、挂了、拿offer，可加日期和一句话备注。面试复盘记下岗位、被问的问题、结果、对方原话、薪资阶段，以及下次要改的一点。生成建议时带上最近 3 条结局（面试复盘优先，不够时用状态为「挂了」的投递补上），并在结果里列出参考了哪些历史。

## 他们碰到的入口

| 入口 | 用来做什么 |
| --- | --- |
| 扩展弹窗 | 看当前页、收藏或导出、打开收藏库、求职建议、填写当前页面、简历和模型 |
| 页面右侧竖条「岗位」 | 在详情页或列表页就地提取、收藏、看简历、导入其他网站的岗位；左缘可拖动宽度，双击恢复默认 |
| 收藏的岗位 | 搜索、按状态筛选、导入、导出、清空 |
| 简历和模型 | 保存多份简历并指定默认；填写接口地址、模型名称、密钥，保存时授权该地址 |
| 求职建议 | 写期望条件、挑选收藏、生成四类建议、记面试复盘 |

沿用这些已经出现的说法：收藏、本页岗位、重新扫描、重新提取、填写当前页面、求职建议、默认简历、导入岗位、面试复盘。

## 界面怎么说

界面、按钮、空状态和错误都用中文（zh-CN），短句写清这一页能做什么、下一步点哪里。

说失败时带上原因和他们能做的下一步：安全验证要先在网页里完成；薪资被网页用自定义字体加密时，让他们直接看页面上的数字；PDF 抽不出文字时，让他们把正文贴进简历。涉及提交、打招呼或投递时，写明这一步由他们自己完成。

## 他们已经知道的边界

这些是 `README.md` 里的承诺。改功能时保持这些结果；代码落点见 `AGENTS.md` 的「必须遵守的产品约束」。

- 提取、收藏、填写、建议都在他们点击之后发生。提交、打招呼、投递和安全验证留在网页里由他们处理。
- 性别、民族、宗教、残疾、政治面貌保持空白，等他们自己选。
- 建议只依据所选简历里已经写明的内容，以及收藏、目标和最近的面试或投递记录。
- 状态为「挂了」的岗位仍留在收藏里，并在建议中标出。
- 收藏、简历、建议、面试记录和模型配置只在本机这个浏览器配置里。插件目录中没有默认密钥。
